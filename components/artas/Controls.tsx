'use client';
import {Select,SelectContent,SelectItem,SelectTrigger,SelectValue} from '@/components/ui/select';
import {Slider} from '@/components/ui/slider';
import {palette} from '@/lib/artas/catalog';
export function Choice({label,value,options,onChange}:{label:string;value:string;options:{value:string;label:string}[];onChange:(v:string)=>void}){return <div className="control-field"><label>{label}</label><Select value={value} onValueChange={onChange}><SelectTrigger aria-label={label} className="artas-select"><SelectValue/></SelectTrigger><SelectContent className="artas-select-menu">{options.map(o=><SelectItem value={o.value} key={o.value}>{o.label}</SelectItem>)}</SelectContent></Select></div>}
export function Range({label,value,min=0,max=100,step=1,unit='%',onChange}:{label:string;value:number;min?:number;max?:number;step?:number;unit?:string;onChange:(n:number)=>void}){return <div className="control-field"><div className="range-label"><label>{label}</label><output>{Number(value.toFixed(2))}{unit}</output></div><Slider aria-label={label} min={min} max={max} step={step} value={[value]} onValueChange={([v])=>onChange(v)}/></div>}
export function Colors({index,onChange}:{index:number;onChange:(i:number)=>void}){return <div className="control-field"><div className="range-label"><label>Kolor wykończenia</label><span>{palette[index].ral==='dekor'?'Dekor':'RAL '+palette[index].ral}</span></div><div className="swatches" role="group" aria-label="Kolory wykończenia">{palette.map((p,i)=><button key={p.name} aria-label={p.name+', '+(p.ral==='dekor'?'dekor':'RAL '+p.ral)} aria-pressed={i===index} title={p.name} className={i===index?'selected':''} style={{background:p.hex}} onClick={()=>onChange(i)}>{i===index&&<span style={{color:i===2||i===3||i===7||i===8?'#181818':'#fff'}}>✓</span>}</button>)}</div><div className="color-name">{palette[index].name}<small>Odcień na ekranie jest orientacyjny.</small></div></div>}
